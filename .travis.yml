sudo: false

language: java

jdk:
  - oraclejdk8

notifications:
  email:
    recipients:
      - evelinec@ca.ibm.com
    on_success: always # default: change
    on_failure: always # default: always
    
  slack: 
    rooms:
      - ibm-cloud:ZdQikj3kvG2RcVpcgFkvD6RZ
    on_success: always # default: change
    on_failure: always # default: always

before_install:
 - cd finish

install: mvn clean install
